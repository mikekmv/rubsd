$RuOBSD$
--- src/ircd.c.orig	Fri Dec  9 11:52:53 2005
+++ src/ircd.c	Wed Dec 21 16:55:27 2005
@@ -168,7 +168,7 @@ make_daemon(void)
   }
   else if (pid > 0)
   {
-    print_startup(pid);
+    /* print_startup(pid); */
     exit(EXIT_SUCCESS);
   }
 
@@ -544,8 +544,18 @@ main(int argc, char *argv[])
 #ifndef _WIN32
   if (geteuid() == 0)
   {
-    fprintf(stderr, "Don't run ircd as root!!!\n");
-    return(-1);
+    struct passwd *pw;
+
+    if ((pw = getpwnam(IRCD_USER)) == NULL)
+    {
+      fprintf(stderr, "No passwd entry for %s!\n", IRCD_USER);
+      return -1;
+    }
+    if (setusercontext(NULL, pw, pw->pw_uid, LOGIN_SETALL) < 0)
+    {
+      fprintf(stderr, "Can's set user context to ircd!\n");
+      return -1;
+    }
   }
 
   /* Setup corefile size immediately after boot -kre */
