$RuOBSD$
--- ircd/send.c.orig	Mon Jan  9 19:07:07 2006
+++ ircd/send.c	Mon Jan  9 19:07:32 2006
@@ -697,7 +697,11 @@ void vsendto_opmask_butone(struct Client
    * this is ok...
    */
   vd.vd_format = pattern;
+#ifdef __OpenBSD__
+  va_copy(vd.vd_args, vl);
+#else
   vd.vd_args = vl;
+#endif
   mb = msgq_make(0, ":%s " MSG_NOTICE " * :*** Notice -- %v", cli_name(&me),
 		 &vd);
 
