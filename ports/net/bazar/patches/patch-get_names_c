$RuOBSD$
--- get_names.c.orig	Mon Jul 30 12:02:13 2001
+++ get_names.c	Mon Jul 30 12:01:58 2001
@@ -70,11 +70,8 @@ get_names(argc, argv)
 		high_print = 1;
 	}
 
-	if ((argc < 2 ) || ('@' == argv[1][0])) {
-		fprintf(stderr, "usage: talk user [ttyname]\n"
-				"       talk user@hostname [ttyname]\n");
-		exit(1);
-	}
+	if (argc < 2 || argc > 3 || '@' == argv[1][0])
+		usage();
 	if (!isatty(0))
 		errx(1, "standard input must be a tty, not a pipe or a file");
 
