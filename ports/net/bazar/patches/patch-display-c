--- display.c.orig	Thu May 18 18:17:54 2000
+++ display.c	Thu May 18 18:29:41 2000
@@ -48,6 +48,8 @@
 #include "talk.h"
 #include <ctype.h>
 
+#include "charset.h"
+
 xwin_t	my_win;
 xwin_t	his_win;
 WINDOW	*line_win;
@@ -92,7 +94,7 @@
 			continue;
 		}
 		/* erase character */
-		if (*text == win->cerase) {
+		if (*text == '\b' || *text == 0177) {
 			wmove(win->x_win, win->x_line, max(--win->x_col, 0));
 			getyx(win->x_win, win->x_line, win->x_col);
 			waddch(win->x_win, ' ');
@@ -106,7 +108,7 @@
 		 * the beginning of a word or the beginning of
 		 * the line.
 		 */
-		if (*text == win->werase) {
+		if (*text == 027) {
 			int endcol, xcol, i, c;
 
 			endcol = win->x_col;
@@ -132,7 +134,7 @@
 			continue;
 		}
 		/* line kill */
-		if (*text == win->kill) {
+		if (*text == 025) {
 			wmove(win->x_win, win->x_line, 0);
 			wclrtoeol(win->x_win);
 			getyx(win->x_win, win->x_line, win->x_col);
@@ -149,7 +151,7 @@
 			/* check for wraparound */
 			xscroll(win, 0);
 		}
-		if (!isprint(*text) && *text != '\t') {
+		if (!ISPRINT(*text) && *text != '\t') {
 			waddch(win->x_win, '^');
 			getyx(win->x_win, win->x_line, win->x_col);
 			if (win->x_col == COLS-1) /* check for wraparound */
@@ -157,7 +159,7 @@
 			cch = (*text & 63) + 64;
 			waddch(win->x_win, cch);
 		} else
-			waddch(win->x_win, *text);
+			waddch(win->x_win, (unsigned char)*text);
 		getyx(win->x_win, win->x_line, win->x_col);
 		text++;
 	}
@@ -198,11 +200,10 @@
 		win->x_col = 0;
 		return;
 	}
-	win->x_line = (win->x_line + 1) % win->x_nlines;
 	win->x_col = 0;
-	wmove(win->x_win, win->x_line, win->x_col);
-	wclrtoeol(win->x_win);
-	wmove(win->x_win, (win->x_line + 1) % win->x_nlines, win->x_col);
-	wclrtoeol(win->x_win);
+	if (++win->x_line == win->x_nlines) {
+		--win->x_line;
+		scroll(win->x_win);
+	}
 	wmove(win->x_win, win->x_line, win->x_col);
 }
