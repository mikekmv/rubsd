$RuOBSD: patch-etc_clamd_conf,v 1.2 2004/12/07 12:48:43 form Exp $
--- etc/clamd.conf.orig	Mon Oct 11 08:14:30 2004
+++ etc/clamd.conf	Sat Dec 11 20:06:45 2004
@@ -4,14 +4,11 @@
 ##
 
 
-# Comment or remove the line below.
-Example
-
 # Uncomment this option to enable logging.
 # LogFile must be writable for the user running daemon.
 # A full path is required.
 # Default: disabled
-#LogFile /tmp/clamd.log
+LogFile /var/clamav/clamd.log
 
 # By default the log file is locked for writing - the lock protects against
 # running clamd multiple times (if want to run another clamd, please
@@ -31,7 +28,7 @@ Example
 
 # Log time with each message.
 # Default: disabled
-#LogTime
+LogTime
 
 # Also log clean files. Useful in debugging but drastically increases the
 # log size.
@@ -54,7 +51,7 @@ Example
 # This option allows you to save a process identifier of the listening
 # daemon (main thread).
 # Default: disabled
-#PidFile /var/run/clamd.pid
+PidFile /var/clamav/clamd.pid
 
 # Optional path to the global temporary directory.
 # Default: system specific (usually /tmp or /var/tmp).
@@ -62,14 +59,14 @@ Example
 
 # Path to the database directory.
 # Default: hardcoded (depends on installation options)
-#DatabaseDirectory /var/lib/clamav
+#DatabaseDirectory /var/clamav/db
 
 # The daemon works in a local OR a network mode. Due to security reasons we
 # recommend the local mode.
 
 # Path to a local socket file the daemon will listen on.
 # Default: disabled
-LocalSocket /tmp/clamd
+LocalSocket /var/clamav/clamd.sock
 
 # Remove stale socket after unclean shutdown.
 # Default: disabled
@@ -130,11 +127,11 @@ FixStaleSocket
 
 # Run as a selected user (clamd must be started by root).
 # Default: disabled
-#User clamav
+User _clamav
 
 # Initialize supplementary group access (clamd must be started by root).
 # Default: disabled
-#AllowSupplementaryGroups
+AllowSupplementaryGroups
 
 # Don't fork into background.
 # Default: disabled
@@ -179,7 +176,7 @@ FixStaleSocket
 
 # This option enables scanning of Microsoft Office document macros.
 # Default: enabled
-#ScanOLE2
+ScanOLE2
 
 ##
 ## Mail files
@@ -187,7 +184,7 @@ FixStaleSocket
 
 # Enable internal e-mail scanner.
 # Default: enabled
-#ScanMail
+ScanMail
 
 # If an email contains URLs ClamAV can download and scan them.
 # WARNING: This option may open your system to a DoS attack.
@@ -202,7 +199,7 @@ FixStaleSocket
 
 # Perform HTML normalisation and decryption of MS Script Encoder code.
 # Default: enabled
-#ScanHTML
+ScanHTML
 
 
 ##
@@ -211,7 +208,7 @@ FixStaleSocket
 
 # ClamAV can scan within archives and compressed files.
 # Default: enabled
-#ScanArchive
+ScanArchive
 
 # Due to license issues libclamav does not support RAR 3.0 archives (only the
 # old 2.0 format is supported). Because some users report stability problems
