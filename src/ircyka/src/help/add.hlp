(P) ADD <nick> [+<flags>] <passwd>
    - Добавить <nick> с паролем <passwd> в базу данных.

    Параметр <flags> может содержать следующие флаги:

    A - Автоматически устанавливать статус оператора/голоса на
        каналах при регистрации в IRCyka и входе на канал.
    C - Разрешить вывод результатов выполнения команд на канал
        в случае если команда выдана на канале.
    P - Разрешить выполнение привилегированных команд.
