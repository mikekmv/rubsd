#	$RuOBSD$

PROG=	tunneld
SRCS=	tunneld.c
MAN=

COPTS=	-Wall -Werror -DHAS_PIDFILE -DCRYPT -DNAT -I${.CURDIR}/../libalias
LDADD=	-lutil -L${.CURDIR}/../libalias/obj -L${.CURDIR}/../libalias -lalias
DPADD=	${LIBUTIL}

BINDIR?=/usr/local/sbin
