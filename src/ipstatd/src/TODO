#
#		$Id$
#

- Reliable communication
- configuration file parsing.
- command line options for dumpstat:
	host,
	port.
- code cleanup & security auditing
	strlen -> memchr
- improve signal handling and use restarted sys calls.
- control kernel buffer overload.
- carefully rewrite parse_ip()
- gnu configure.
- Portability
- ipchains support.
- libpcap support
- profiling & optimization.
- BSD port.
- statistic format via config
- flex configuration for per interface accounting...
- documentation :)

Fix some BUGS and add futures to ipfilter logging:
- poll & select must work properly.
- ipl must be accessible by many processes.
- logging rewriting ( breakage linked list )
